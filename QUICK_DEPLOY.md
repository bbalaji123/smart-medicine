# 🚀 Quick Deploy to Render - 5 Minute Guide

## Prerequisites (5 minutes)
1. **MongoDB Atlas** - Create free cluster at [mongodb.com/cloud/atlas](https://mongodb.com/cloud/atlas)
   - Get connection string (looks like: `mongodb+srv://user:pass@cluster.mongodb.net/smart-medicine`)
   - Set IP whitelist to `0.0.0.0/0` (allow all)

2. **GitHub** - Push your code
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

## Deploy (2 minutes)

### Option 1: One-Click Blueprint ⚡ (Easiest)
1. Go to [dashboard.render.com](https://dashboard.render.com)
2. Click **New** → **Blueprint**
3. Connect your GitHub repo
4. Render auto-detects `render.yaml`
5. Click **Apply**
6. Wait 5-10 minutes for deployment

### Option 2: Manual Setup 🔧
1. **Backend**:
   - New → Web Service
   - Root: `backend`
   - Build: `npm install`
   - Start: `npm start`

2. **Frontend**:
   - New → Static Site
   - Root: `frontend`
   - Build: `npm install && npm run build`
   - Publish: `build`

## Configure (1 minute)

1. **Add MongoDB URI** to backend:
   - Go to backend service → Environment
   - Add: `MONGODB_URI` = `your-mongodb-connection-string`
   - Save (triggers redeploy)

2. **Update Frontend API URL** (if manual setup):
   - Go to frontend service → Environment
   - Add: `REACT_APP_API_URL` = `https://your-backend-url.onrender.com/api`
   - Save (triggers redeploy)

3. **Update Backend CORS**:
   - Go to backend service → Environment
   - Update `CORS_ORIGINS` = `https://your-frontend-url.onrender.com`
   - Update `CLIENT_URL` = `https://your-frontend-url.onrender.com`
   - Save

## Test (1 minute)

1. **Backend Health**: Visit `https://your-backend-url.onrender.com/health`
   - Should see: `{"success": true, "status": "OK"}`

2. **Frontend**: Visit `https://your-frontend-url.onrender.com`
   - Try signup/login

## Done! 🎉

Your app is live!

---

## Environment Variables Cheat Sheet

### Backend (Required)
```
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/smart-medicine
JWT_SECRET=(auto-generated by Render)
CORS_ORIGINS=https://your-frontend.onrender.com
CLIENT_URL=https://your-frontend.onrender.com
```

### Frontend (Required)
```
REACT_APP_API_URL=https://your-backend.onrender.com/api
```

---

## Troubleshooting

**❌ CORS errors?**
- Update `CORS_ORIGINS` in backend with exact frontend URL
- No trailing slashes!

**❌ API not found?**
- Check `REACT_APP_API_URL` ends with `/api`
- Verify backend is running (check health endpoint)

**❌ MongoDB connection failed?**
- Verify connection string
- Check IP whitelist is `0.0.0.0/0`

**🐌 App slow to wake up?**
- Free tier services sleep after 15 min inactivity
- First request takes 30-60 seconds to wake up

---

## URLs You'll Get

After deployment, you'll have:
- Backend: `https://smart-medicine-backend.onrender.com`
- Frontend: `https://smart-medicine-frontend.onrender.com`

(Names depend on what you choose during setup)

---

## Important Notes

- 🆓 Free tier includes 750 hours/month
- 💤 Services sleep after 15 min inactivity
- 🔄 Auto-deploys on git push
- 📊 View logs in Render dashboard
- 🌐 Add custom domain in Render settings (free)

---

**Need detailed instructions?** See `RENDER_DEPLOYMENT_CHECKLIST.md`
**Having issues?** Check `DEPLOYMENT.md` troubleshooting section
